allprojects {
    repositories {
        mavenCentral()
    }
    group 'com.robwettach.webdiplomacy'
    version '0.1'

    tasks.withType(Jar) {
        archivesBaseName = "webdiplomacy-$project.name"
    }
}
